{
  "name": "basic-auth-examples",
  "version": "0.1.0",
  "description": "Basic Authentication Examples",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report $(if [ \"$TRAVIS\" ]; then echo lcovonly; else echo html; fi) -- && if [ \"$TRAVIS\" ]; then cat ./coverage/lcov.info | ./node_modules/.bin/coveralls; fi"
  },
  "author": "Whitney Young",
  "license": "MIT",
  "dependencies": {
    "express": "~4.1.1"
  },
  "devDependencies": {
    "istanbul": "~0.2.7",
    "coveralls": "~2.10.0",
    "mocha": "~1.18.2",
    "chai": "^1.9.1",
    "sinon": "^1.9.1"
  }
}
